{{? data.response.links }}

#### Links

{{ for (var l in data.response.links) { }}
{{ var link = data.response.links[l]; }}

**{{=l}}** => <a href="#">{{=link.operationId||link.operationRef}}</a> 

{{? link.parameters }}
|Parameter|Expression|
|---|---|
{{for (var p in link.parameters) { }}|{{=p}}|{{=link.parameters[p]}}|{{ } }}
{{?}}

{{ } /* of links */ }}

{{?}}
